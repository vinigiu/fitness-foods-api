import fs from "fs";
import readline from "readline";
import Path from "path";

export default async function importProducts() {
  const importInfo = {};
  const imported_t = new Date().toISOString;

  try {
    // TODO: Incluir todas as etapas do processo de importação
  } catch (error) {
    console.log(error);
  }
}
